services:
  - type: web
    name: supabase-mcp-server
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: node dist/sse-server.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /

